# Tailscale Configuration
# Replace with your actual Tailscale hostname or IP
# You can find your IP with: tailscale ip -4
# Or use your machine name: your-machine-name.tailnet-name.ts.net

# Example with Tailscale hostname:
# VITE_API_URL=http://your-machine.tailnet.ts.net:8001

# Example with IP (replace YOUR_TAILSCALE_IP with actual IP):
# VITE_API_URL=http://YOUR_TAILSCALE_IP:8001

# Default to localhost for now - update with your Tailscale details
VITE_API_URL=http://localhost:8001