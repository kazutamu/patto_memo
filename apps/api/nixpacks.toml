# Nixpacks configuration for Railway deployment
# This file forces Railway to detect this as a Python project

[providers]
python = "3.11"

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"